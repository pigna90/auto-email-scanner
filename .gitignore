# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/

# runtime data
/scans/
work/
*.pdf

# secrets (the .example is tracked; the real file must never be)
telegram.env
*.env
!*.env.example
